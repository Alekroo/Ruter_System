How to run:

1.Type "make" to use the makefile to compile the code.
2. A new file will be created called "test.exe", execute it through your teminal by typing "./test [filename]" in which [filename] is one of the .dat files provided in the folder.  (tested on Linux MInt).

